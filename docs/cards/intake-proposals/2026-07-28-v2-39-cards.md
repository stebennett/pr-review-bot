# Proposed backlog v2 — modular refactor of pr-review-bot

Rework of v1 against intake check run 1 (`docs/cards/intake-checks/2026-07-28-modular-refactor.md`),
which returned **fail** on INT-SIZED, INT-MILESTONE and INT-NO-OVERLAP.

Spec: `docs/superpowers/specs/2026-07-28-modular-refactor-design.md` — now **28** requirements
(REQ-028 added; REQ-010 corrected; REQ-016 amended).
Board: still empty. These 39 cards are the whole board.
Layers: `infra`, `core`, `adapters`, `app`. `gate_layer: core`. `size_limit: 500`.
`size_exclude` now includes `tests/goldens/**` (finding A1, applied).

## How v1's blocking findings were discharged

- **B1 CARD-004 (~9,000)** — `tests/goldens/**` excluded, removing the golden volume from the
  count. The remaining hand-written ~1,650 lines are **deliberately kept as one card** by driver
  override (CARD-005 below), marked `right_sized: true`. Recorded as an accepted breach, not hidden.
- **B2, B3, B5, B6, B7, B8** — split along the lines the checker named.
- **B9 CARD-023 (~5,000)** — reduced to the pytest runner migration alone; each test module's
  relocation now travels with the extraction card that moves its production code.
- **B10 REQ-010 mis-scheduled** — discharged by the spec amendment: the obligation now belongs to
  whichever card moves each offending function into `core/`. `Accounting.report()` → CARD-009;
  `resolve_requirements` → CARD-020. `extract_checkout` needs no work (already uses `log()`).
- **B11 CARD-016/019 contradiction** — discharged by the REQ-016 amendment: `openrouter` is
  ported to httpx, gated on a live cache measurement (CARD-029), not frozen.
- **A4** — "goldens byte-identical" ACs now cite REQ-022, not REQ-001.
- **A8** — test-conversion ownership stated explicitly on every card that has one.

---

# M1 — Pipeline and safety net

**Goal:** Close the deployment gap and make every later pull request independently validated, hermetically.
**Exit:** A tagged build publishes a working image; CI runs tests, a hermetic image smoke-test and the golden comparison on every PR.

## CARD-001 — CI runs the test suite on every pull request
task | infra | reqs: [REQ-018] | depends_on: [] | est: 60
- [ ] `.github/workflows/ci.yml` runs the suite on every PR and reports status (REQ-018)
- [ ] A deliberately failing test shows a red check (REQ-018)
- [ ] The workflow makes no network call to GitHub's or OpenRouter's real APIs (REQ-028)

## CARD-002 — Base URLs default from the environment, with a stub server for tests
task | adapters | reqs: [REQ-028] | depends_on: [CARD-001] | est: 260
- [ ] `GITHUB_API` and `OPENROUTER_API` default from environment variables, preserving today's values (REQ-028)
- [ ] All seven call sites (`review.py:216, 288, 291, 296, 301, 314, 492`) resolve through them (REQ-028)
- [ ] A stub HTTP server fixture serves canned GitHub and OpenRouter responses (REQ-028)
- [ ] A test drives a full offline review against the stub with no real network call (REQ-028)

## CARD-003 — Container image, built and hermetically smoke-tested on every PR
task | infra | reqs: [REQ-017, REQ-018, REQ-019, REQ-020, REQ-021, REQ-028] | depends_on: [CARD-002] | est: 190
- [ ] `Dockerfile` produces an image whose entrypoint is `python3 /app/review.py` (REQ-019)
- [ ] `doctrine/` is present inside the image (REQ-020)
- [ ] Bytecode is precompiled at build; the container runs with `--read-only` (REQ-021)
- [ ] `requirements.txt` and `requirements-dev.txt` exist; no dev dependency is in the image (REQ-017)
- [ ] CI builds the image and runs a full review against the stub server, not `--help` (REQ-018, REQ-028)

## CARD-004 — Publish the image to GHCR on a tagged release
task | infra | reqs: [REQ-018] | depends_on: [CARD-003] | est: 70
- [ ] `release.yml` pushes `ghcr.io/stebennett/pr-review-bot:X.Y.Z` on a version tag (REQ-018)
- [ ] The published image passes the same hermetic smoke-test CI runs (REQ-018, REQ-028)

## CARD-005 — Synthetic fixtures and the golden prompt-byte harness
task | infra | reqs: [REQ-022, REQ-023, REQ-024] | depends_on: [CARD-001] | est: 1650 | right_sized: true
> **Driver override, recorded:** this card is ~1,650 counted lines against a 500 ceiling and is
> deliberately kept whole. The fixture corpus, the harness and the first goldens are useless
> apart — a harness with no fixtures pins nothing, and fixtures with no harness assert nothing.
> `right_sized: true` so the slice phase does not re-split it. Goldens themselves are excluded
> from the count via `tests/goldens/**`.
- [ ] Five committed fixtures cover budget-filling, hunk windowing, the dropped-file trailer, adversarial backtick fences, and an all-new-files PR (REQ-023)
- [ ] Goldens record, byte for byte, three lens system+user blocks, the verdict prompt, and `ctx.pack`/`ctx.notes`/`ctx.requirements` (REQ-022)
- [ ] A test regenerates and fails on any byte difference (REQ-024)
- [ ] The harness makes zero network calls and no fixture depends on anything outside the repository (REQ-023, REQ-028)

# M2 — Pure core extracted

**Goal:** The pure, I/O-free logic lives in `reviewer/core/` behind an enforced import boundary.
**Exit:** Import-boundary test passes; both prompt invariants structural; goldens byte-identical.

## CARD-006 — Package skeleton, config, log, ports, and the import-boundary test
task | core | reqs: [REQ-001, REQ-009, REQ-024] | depends_on: [CARD-005] | est: 310
- [ ] `reviewer/config.py` holds `DEFAULTS` and pure resolution with no `.env` side effect (REQ-001)
- [ ] An AST test fails if `core/` imports `adapters`, `app` or `log`, or `adapters/` imports `app` (REQ-009)
- [ ] CI runs the import-boundary test (REQ-024)
- [ ] Goldens byte-identical (REQ-022)

## CARD-007 — Extract `core/target`, `core/marker`, `core/schemas`
task | core | reqs: [REQ-001] | depends_on: [CARD-006] | est: 250
- [ ] `parse_target`, marker parse/compose and the three schemas live in `reviewer/core/` (REQ-001)
- [ ] Their tests move with them (REQ-025)
- [ ] Goldens byte-identical (REQ-022)

## CARD-008 — Extract `core/diff`
task | core | reqs: [REQ-001] | depends_on: [CARD-006] | est: 420
- [ ] `_hunks`, `diff_paths`, `diff_anchors`, `anchor_violations`, `diff_size` in `core/diff.py` (REQ-001)
- [ ] `tests/test_diff.py` moves to `tests/core/` with them (REQ-025)
- [ ] Goldens byte-identical (REQ-022)

## CARD-009 — Extract `core/budget`; `Accounting` returns diagnostics as data
task | core | reqs: [REQ-001, REQ-010, REQ-011] | depends_on: [CARD-006] | est: 400
- [ ] `_capped` is the only budget-slicing code in the package, assertable by AST (REQ-011)
- [ ] `Accounting.report()` returns per-part usage as data; `app/` emits it; `core/budget.py` does not import `reviewer.log` (REQ-010)
- [ ] The existing sweep over every integer budget still passes (REQ-011)
- [ ] Goldens byte-identical (REQ-022)

## CARD-010 — Extract `core/render`
task | core | reqs: [REQ-001] | depends_on: [CARD-006] | est: 250
- [ ] `merge_ranges`, `windowed`, `is_binary`, `path_matches` in `core/render.py` (REQ-001)
- [ ] Goldens byte-identical (REQ-022)

## CARD-011 — Extract `core/prompt`; the cache prefix becomes inexpressible
task | core | reqs: [REQ-001, REQ-007, REQ-022] | depends_on: [CARD-007, CARD-008, CARD-009, CARD-010] | est: 400
- [ ] `build_shared_blocks` has no `lens` parameter, verified by `inspect.signature` (REQ-007)
- [ ] `build_lens_tail(lens, brief)` builds block 3 alone (REQ-007)
- [ ] Goldens byte-identical across all three lenses (REQ-022)

## CARD-012 — Extract `core/consistency`; the adjudicator cannot see the diff or pack
task | core | reqs: [REQ-001, REQ-008] | depends_on: [CARD-011] | est: 320
- [ ] `build_verdict_prompt` has no parameter able to carry the diff or pack, verified by `inspect.signature` (REQ-008)
- [ ] Its module imports no pack type, verified by the AST test (REQ-008)
- [ ] The I4 mutation tests still fail when diff or pack is appended via `run_panel` (REQ-008)

# M3 — The pack behind a port

**Goal:** The degradation ladder becomes a choice of `SourceTree` implementation.
**Exit:** No assembler contains `root is None` branching; all four rungs are implementations.

## CARD-013 — `SourceTree` port, `LocalTree`, `EmptyTree`, `InMemoryTree`
task | core | reqs: [REQ-003] | depends_on: [CARD-006] | est: 440
> Owns only the port and the three implementations. `walk_source`/`grep_repo`/`_clip_hit` move in CARD-017.
- [ ] `ports.py` defines `SourceTree` with `read`, `exists`, `walk`, `grep` (REQ-003)
- [ ] `read()` returns `None` rather than raising on any failure (REQ-003)
- [ ] `LocalTree`, `EmptyTree` and an `InMemoryTree` test fake implement it (REQ-003)

## CARD-014 — File reading and windowing against the port
task | core | reqs: [REQ-004, REQ-022] | depends_on: [CARD-013] | est: 330
- [ ] `read_source`, `_whole_file_body`, `_file_body`, `_file_tiers` take a `SourceTree` (REQ-004)
- [ ] No `root is None` branching remains in them, assertable by grep (REQ-004)
- [ ] Goldens byte-identical (REQ-022)

## CARD-015 — The changed-files allocator against the port
task | core | reqs: [REQ-004, REQ-022] | depends_on: [CARD-014] | est: 350
> Highest-risk module in the repo — five fix rounds of history. Behaviour must not change.
- [ ] `pack_changed_files`, `_stretch`, `_trailer_groups` take a `SourceTree` (REQ-004)
- [ ] Greedy admission, give-back and re-admission behaviour is unchanged (REQ-004)
- [ ] Goldens byte-identical on the budget-filling and windowing fixtures (REQ-022)

## CARD-016 — Convert the changed-files tests to `InMemoryTree`
task | core | reqs: [REQ-004, REQ-025] | depends_on: [CARD-015] | est: 350
- [ ] `TestPackChangedFiles` uses `InMemoryTree`, not a temp directory (REQ-025)
- [ ] The tests live under `tests/core/pack/` (REQ-025)
- [ ] Coverage of the allocator is no lower than before (REQ-004)

## CARD-017 — `pack/call_sites` against the port
task | core | reqs: [REQ-004, REQ-022] | depends_on: [CARD-013] | est: 440
> Owns `walk_source`, `grep_repo`, `_clip_hit`. Test conversion is CARD-018, not here.
- [ ] `changed_symbols` and `pack_call_sites` take a `SourceTree` (REQ-004)
- [ ] `CODE_SUFFIXES` restriction and the symbol hit ceiling are unchanged (REQ-004)
- [ ] Goldens byte-identical (REQ-022)

## CARD-018 — Convert the call-sites tests to `InMemoryTree`
task | core | reqs: [REQ-004, REQ-025] | depends_on: [CARD-017] | est: 320
- [ ] `TestGrepRepo`, `TestPackCallSites`, `TestPackCallSitesDeterminism` use `InMemoryTree` (REQ-025)
- [ ] Determinism assertions still hold — needles built from ordered structures, never a `set` (REQ-004)

## CARD-019 — `pack/conventions` and `pack/tree` against the port
task | core | reqs: [REQ-004, REQ-022] | depends_on: [CARD-013] | est: 400
- [ ] Both take a `SourceTree` with no `root is None` branching (REQ-004)
- [ ] Nearest-first convention ordering is unchanged (REQ-004)
- [ ] Goldens byte-identical on the adversarial-fence fixture (REQ-022)

## CARD-020 — `pack/requirements`, the `IssueSource` port, and fetch failures as notes
task | core | reqs: [REQ-004, REQ-006, REQ-010] | depends_on: [CARD-013] | est: 330
- [ ] `ports.py` defines `IssueSource` with `issue` and `comments` only (REQ-006)
- [ ] `core/pack/requirements.py` imports no write client, verified by the AST test (REQ-006)
- [ ] Fetch failures are recorded as notes, visible without `-v`; the module does not import `reviewer.log` (REQ-010)
- [ ] Goldens byte-identical including `ctx.requirements` (REQ-022)

## CARD-021 — `TarballTree` and `ApiTree` complete the ladder
task | adapters | reqs: [REQ-003, REQ-012] | depends_on: [CARD-013] | est: 380
- [ ] All four rungs are `SourceTree` implementations (REQ-003)
- [ ] `ApiTree` serves `read()` and returns emptiness from `walk`/`grep`; the pack still states in band what is missing (REQ-003)
- [ ] `tarfile`'s `filter="data"` is preserved (REQ-012)

# M4 — Adapters isolated

**Goal:** Every side effect lives at the edge.
**Exit:** No `core/` module logs or performs I/O.

## CARD-022 — Extract `adapters/env`, `adapters/doctrine`, `adapters/auth`
task | adapters | reqs: [REQ-001] | depends_on: [CARD-006] | est: 290
- [ ] `.env` loading lives in `adapters/env.py`, not `config.py` (REQ-001)
- [ ] `Doctrine` loading and token resolution live in adapters (REQ-001)

## CARD-023 — Extract `adapters/github` and the HTTP helper
task | adapters | reqs: [REQ-001] | depends_on: [CARD-022] | est: 330
- [ ] `GitHub`, `whoami`, `post_review`, `repo_config`, `prior_rounds` live in `adapters/github.py` (REQ-001)
- [ ] The 422 inline-comment fallback is exercised against the stub server (REQ-028)

## CARD-024 — Relocate `openrouter` behind `ModelClient`
task | adapters | reqs: [REQ-005] | depends_on: [CARD-006] | est: 434
> Relocation only. The httpx port is CARD-028; the cache gate is CARD-029.
- [ ] `ports.py` defines `ModelClient` (REQ-005)
- [ ] Request shape is unchanged: `cache_control` blocks, `provider.require_parameters`, retry ladder, `cached_tokens` logging (REQ-005)

## CARD-025 — `RecordingModelClient` and panel dispatch tests
task | adapters | reqs: [REQ-005] | depends_on: [CARD-024] | est: 210
- [ ] A `RecordingModelClient` fake implements `ModelClient` (REQ-005)
- [ ] Staggered dispatch, the retry ladder and per-lens failure handling are tested with no network call (REQ-005, REQ-028)

# M5 — Dependencies adopted

**Goal:** Hand-rolled transport and auth replaced where a library is better, with the cache invariant measured.
**Exit:** No `openssl` subprocess; explicit timeouts; cache measurement recorded.

## CARD-026 — Sign App JWTs with PyJWT; delete the `openssl` shell-out
task | adapters | reqs: [REQ-013, REQ-014, REQ-028] | depends_on: [CARD-022] | est: 200
- [ ] No `subprocess` call remains in the auth path (REQ-014)
- [ ] The JWT verifies against a key pair generated in the test, asserting `iss`, `iat`, `exp` and `alg=RS256` (REQ-028)
- [ ] `PyJWT` and `cryptography` pinned in `requirements.txt` (REQ-013)

## CARD-027 — Adopt httpx for the GitHub transport
task | adapters | reqs: [REQ-013] | depends_on: [CARD-023, CARD-026] | est: 320
- [ ] GitHub calls go through httpx with an explicit timeout on every request (REQ-013)
- [ ] Tests drive them against the stub server (REQ-028)

## CARD-028 — Port the OpenRouter transport to httpx
task | adapters | reqs: [REQ-013, REQ-016] | depends_on: [CARD-024, CARD-027] | est: 260
> Edits only the transport call (`review.py:492`) and its exception type (`:493`). No SDK.
- [ ] `_http` is gone from the package (REQ-013)
- [ ] A test pins three retry attempts on the same status codes as today (REQ-016)
- [ ] Request shape byte-identical: `cache_control` blocks, `provider.require_parameters`, `cached_tokens` logging (REQ-016)

## CARD-029 — Cache-measurement gate for the OpenRouter port
task | adapters | reqs: [REQ-016] | depends_on: [CARD-028] | est: 120
> The one paid, non-hermetic verification in this plan. Never a CI job (REQ-028). Gates CARD-028 landing.
- [ ] A live run reports non-zero `cached_tokens` on the second and third lens calls (REQ-016)
- [ ] The reading is consistent with Gate 1's 91% cache-read figure (REQ-016)
- [ ] A zero reading is re-run before being believed, and both readings are recorded on the card (REQ-016)

## CARD-030 — Confirm the GitHub client choice against the real API surface
task | adapters | reqs: [REQ-015] | depends_on: [CARD-027] | est: 90
- [ ] `githubkit`'s App installation auth and 422 handling are verified against the real API (REQ-015)
- [ ] The outcome is recorded: adopt, fall back to `PyGithub`, or stay hand-rolled on httpx (REQ-015)
- [ ] If the fallback is taken, CARD-031 and CARD-032 are closed as superseded rather than left open (REQ-015)

## CARD-031 — Swap the GitHub read path to githubkit
task | adapters | reqs: [REQ-013, REQ-015] | depends_on: [CARD-030] | est: 320
- [ ] PR fetch, issue fetch and comment fetch go through the client (REQ-013)
- [ ] Bot filtering by `type == "Bot"` and `[bot]` suffix is unchanged (REQ-015)

## CARD-032 — Swap the GitHub write path to githubkit
task | adapters | reqs: [REQ-013, REQ-015, REQ-028] | depends_on: [CARD-031] | est: 300
- [ ] Review posting goes through the client (REQ-013)
- [ ] The 422 inline-comment fallback still folds comments into the body, proved against the stub (REQ-028)

# M6 — App wired, monolith retired

**Goal:** `review.py` becomes an entry point rather than the program.
**Exit:** `review.py` is three lines; every documented command works.

## CARD-033 — Extract `app/select`
task | app | reqs: [REQ-001] | depends_on: [CARD-023] | est: 300
- [ ] Candidate selection, triage and round counting live in `app/select.py` (REQ-001)
- [ ] Drafts and Renovate-authored PRs are still never selected (REQ-012)

## CARD-034 — Extract `app/context`
task | app | reqs: [REQ-001, REQ-010] | depends_on: [CARD-021, CARD-020] | est: 420
- [ ] `build_context` selects a `SourceTree` implementation and records a note per degradation (REQ-001)
- [ ] It emits the diagnostics `core/` returned as data (REQ-010)
- [ ] Goldens byte-identical including `ctx.notes` (REQ-022)

## CARD-035 — Extract `app/panel`
task | app | reqs: [REQ-001] | depends_on: [CARD-012, CARD-025] | est: 280
- [ ] Staggered dispatch, per-lens failure handling and consistency enforcement live in `app/panel.py` (REQ-001)
- [ ] An unsound verdict still raises rather than posting (REQ-001)

## CARD-036 — Extract `app/review` and `app/cli`
task | app | reqs: [REQ-001] | depends_on: [CARD-033, CARD-034, CARD-035] | est: 480
- [ ] `review_pr`, `review_diff_file`, the parser and `main` live in `app/` (REQ-001)
- [ ] Posting still requires explicit opt-in and `--dry-run` still wins (REQ-012)

## CARD-037 — Reduce `review.py` to a shim
task | app | reqs: [REQ-002, REQ-012] | depends_on: [CARD-036] | est: 180
- [ ] `review.py` is three lines (REQ-002)
- [ ] Every command in `CLAUDE.md` and `README.md` runs unchanged (REQ-002)
- [ ] No module introduces persistence; round number and last-reviewed SHA come only from `MARKER_RE` (REQ-012)

# M7 — Tests restructured

**Goal:** The suite runs under pytest and mirrors the package.
**Exit:** pytest green in CI; no test lost.

## CARD-038 — Migrate the runner to pytest
task | infra | reqs: [REQ-013, REQ-025] | depends_on: [CARD-037] | est: 200
> Test relocation already happened card by card; this is the runner only.
- [ ] The suite runs under pytest with no test lost, and CI runs it (REQ-013)
- [ ] `conftest.py` provides the `InMemoryTree`, `RecordingModelClient` and stub-server fixtures (REQ-025)
- [ ] `pytest` is in `requirements-dev.txt` and absent from the image (REQ-017)

# M8 — Worktree head verification

**Goal:** Land the one intended behaviour change in isolation.
**Exit:** Stale worktree warns, degrades and still packs; goldens move only in the label.

## CARD-039 — `--worktree` verifies the checkout is at the PR head
task | app | reqs: [REQ-027, REQ-022] | depends_on: [CARD-034] | est: 230
- [ ] A worktree not at the PR head records a note, keeps the conservative label, and still produces a pack (REQ-027)
- [ ] A worktree at the PR head is labelled as such (REQ-027)
- [ ] The label predicate is correct for a rejected worktree that fell back to per-file API fetches (REQ-027)
- [ ] Goldens change in exactly the labelling and nowhere else (REQ-022)
